# Ads Article Website

## Features:
- Admin Dashboard for posting articles.
- Article viewer displaying the latest content.
- Firebase setup to store articles permanently.
- Mobile-friendly design with responsive CSS.

## Setup:
1. Copy all files into the `/ads-website/` directory.
2. Add Firebase credentials in `firebase-config/firebase.js`.
3. Run `index.html` in your browser.
